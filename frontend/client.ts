// Code generated by the Encore 1.56.0 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly admin: admin.ServiceClient
    public readonly auth: auth.ServiceClient
    public readonly config: config.ServiceClient
    public readonly demo: demo.ServiceClient
    public readonly employers: employers.ServiceClient
    public readonly ghl: ghl.ServiceClient
    public readonly jobs: jobs.ServiceClient
    public readonly matching: matching.ServiceClient
    public readonly messages: messages.ServiceClient
    public readonly notifications: notifications.ServiceClient
    public readonly offers: offers.ServiceClient
    public readonly payments: payments.ServiceClient
    public readonly reviews: reviews.ServiceClient
    public readonly sales: sales.ServiceClient
    public readonly support: support.ServiceClient
    public readonly uploads: uploads.ServiceClient
    public readonly users: users.ServiceClient
    public readonly workers: workers.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.admin = new admin.ServiceClient(base)
        this.auth = new auth.ServiceClient(base)
        this.config = new config.ServiceClient(base)
        this.demo = new demo.ServiceClient(base)
        this.employers = new employers.ServiceClient(base)
        this.ghl = new ghl.ServiceClient(base)
        this.jobs = new jobs.ServiceClient(base)
        this.matching = new matching.ServiceClient(base)
        this.messages = new messages.ServiceClient(base)
        this.notifications = new notifications.ServiceClient(base)
        this.offers = new offers.ServiceClient(base)
        this.payments = new payments.ServiceClient(base)
        this.reviews = new reviews.ServiceClient(base)
        this.sales = new sales.ServiceClient(base)
        this.support = new support.ServiceClient(base)
        this.uploads = new uploads.ServiceClient(base)
        this.users = new users.ServiceClient(base)
        this.workers = new workers.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * Import the auth handler to be able to derive the auth type
 */
import type { auth as auth_auth } from "~backend/auth/handler";

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: RequestType<typeof auth_auth> | AuthDataGenerator
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { bootstrap as api_admin_bootstrap_bootstrap } from "~backend/admin/bootstrap";
import {
    createComplianceOfficer as api_admin_compliance_officers_createComplianceOfficer,
    deleteComplianceOfficer as api_admin_compliance_officers_deleteComplianceOfficer,
    listComplianceOfficers as api_admin_compliance_officers_listComplianceOfficers
} from "~backend/admin/compliance_officers";
import { listDemoLeads as api_admin_demo_leads_listDemoLeads } from "~backend/admin/demo_leads";
import { adminSendDocumentMessage as api_admin_document_message_adminSendDocumentMessage } from "~backend/admin/document_message";
import {
    adminCreateEmailTemplate as api_admin_email_comms_adminCreateEmailTemplate,
    adminDeleteEmailTemplate as api_admin_email_comms_adminDeleteEmailTemplate,
    adminListEmailLog as api_admin_email_comms_adminListEmailLog,
    adminListEmailTemplates as api_admin_email_comms_adminListEmailTemplates,
    adminListUserEmailLog as api_admin_email_comms_adminListUserEmailLog,
    adminSendBulkEmail as api_admin_email_comms_adminSendBulkEmail,
    adminSendEmailToUser as api_admin_email_comms_adminSendEmailToUser,
    adminUpdateEmailTemplate as api_admin_email_comms_adminUpdateEmailTemplate
} from "~backend/admin/email_comms";
import {
    adminGrantSubscription as api_admin_employers_adminGrantSubscription,
    adminListEmployers as api_admin_employers_adminListEmployers,
    adminRevokeSubscription as api_admin_employers_adminRevokeSubscription
} from "~backend/admin/employers";
import {
    adminArchiveUser as api_admin_platform_adminArchiveUser,
    adminDeleteUser as api_admin_platform_adminDeleteUser,
    adminGetPlatformStats as api_admin_platform_adminGetPlatformStats,
    adminListJobs as api_admin_platform_adminListJobs,
    adminListUsers as api_admin_platform_adminListUsers,
    adminSuspendUser as api_admin_platform_adminSuspendUser,
    adminUpdateJobStatus as api_admin_platform_adminUpdateJobStatus,
    adminVerifyUserEmail as api_admin_platform_adminVerifyUserEmail
} from "~backend/admin/platform";
import {
    getPrivacyPolicy as api_admin_privacy_policy_getPrivacyPolicy,
    updatePrivacyPolicy as api_admin_privacy_policy_updatePrivacyPolicy
} from "~backend/admin/privacy_policy";
import {
    adminGetReferenceCheck as api_admin_reference_check_adminGetReferenceCheck,
    adminSubmitReferenceCheck as api_admin_reference_check_adminSubmitReferenceCheck
} from "~backend/admin/reference_check";
import { adminResetUserPassword as api_admin_reset_user_password_adminResetUserPassword } from "~backend/admin/reset_user_password";
import { seed as api_admin_seed_seed } from "~backend/admin/seed";
import {
    adminListAuditLog as api_admin_settings_adminListAuditLog,
    adminListSettings as api_admin_settings_adminListSettings,
    adminUpdateSetting as api_admin_settings_adminUpdateSetting
} from "~backend/admin/settings";
import {
    adminGetDocumentDownloadUrl as api_admin_verify_document_adminGetDocumentDownloadUrl,
    adminVerifyDocument as api_admin_verify_document_adminVerifyDocument
} from "~backend/admin/verify_document";
import {
    adminGetWorkerDocuments as api_admin_workers_adminGetWorkerDocuments,
    adminGetWorkerReferences as api_admin_workers_adminGetWorkerReferences,
    adminListWorkers as api_admin_workers_adminListWorkers,
    adminVerifyReference as api_admin_workers_adminVerifyReference
} from "~backend/admin/workers";

export namespace admin {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.adminArchiveUser = this.adminArchiveUser.bind(this)
            this.adminCreateEmailTemplate = this.adminCreateEmailTemplate.bind(this)
            this.adminDeleteEmailTemplate = this.adminDeleteEmailTemplate.bind(this)
            this.adminDeleteUser = this.adminDeleteUser.bind(this)
            this.adminGetDocumentDownloadUrl = this.adminGetDocumentDownloadUrl.bind(this)
            this.adminGetPlatformStats = this.adminGetPlatformStats.bind(this)
            this.adminGetReferenceCheck = this.adminGetReferenceCheck.bind(this)
            this.adminGetWorkerDocuments = this.adminGetWorkerDocuments.bind(this)
            this.adminGetWorkerReferences = this.adminGetWorkerReferences.bind(this)
            this.adminGrantSubscription = this.adminGrantSubscription.bind(this)
            this.adminListAuditLog = this.adminListAuditLog.bind(this)
            this.adminListEmailLog = this.adminListEmailLog.bind(this)
            this.adminListEmailTemplates = this.adminListEmailTemplates.bind(this)
            this.adminListEmployers = this.adminListEmployers.bind(this)
            this.adminListJobs = this.adminListJobs.bind(this)
            this.adminListSettings = this.adminListSettings.bind(this)
            this.adminListUserEmailLog = this.adminListUserEmailLog.bind(this)
            this.adminListUsers = this.adminListUsers.bind(this)
            this.adminListWorkers = this.adminListWorkers.bind(this)
            this.adminResetUserPassword = this.adminResetUserPassword.bind(this)
            this.adminRevokeSubscription = this.adminRevokeSubscription.bind(this)
            this.adminSendBulkEmail = this.adminSendBulkEmail.bind(this)
            this.adminSendDocumentMessage = this.adminSendDocumentMessage.bind(this)
            this.adminSendEmailToUser = this.adminSendEmailToUser.bind(this)
            this.adminSubmitReferenceCheck = this.adminSubmitReferenceCheck.bind(this)
            this.adminSuspendUser = this.adminSuspendUser.bind(this)
            this.adminUpdateEmailTemplate = this.adminUpdateEmailTemplate.bind(this)
            this.adminUpdateJobStatus = this.adminUpdateJobStatus.bind(this)
            this.adminUpdateSetting = this.adminUpdateSetting.bind(this)
            this.adminVerifyDocument = this.adminVerifyDocument.bind(this)
            this.adminVerifyReference = this.adminVerifyReference.bind(this)
            this.adminVerifyUserEmail = this.adminVerifyUserEmail.bind(this)
            this.bootstrap = this.bootstrap.bind(this)
            this.createComplianceOfficer = this.createComplianceOfficer.bind(this)
            this.deleteComplianceOfficer = this.deleteComplianceOfficer.bind(this)
            this.getPrivacyPolicy = this.getPrivacyPolicy.bind(this)
            this.listComplianceOfficers = this.listComplianceOfficers.bind(this)
            this.listDemoLeads = this.listDemoLeads.bind(this)
            this.seed = this.seed.bind(this)
            this.updatePrivacyPolicy = this.updatePrivacyPolicy.bind(this)
        }

        public async adminArchiveUser(params: RequestType<typeof api_admin_platform_adminArchiveUser>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                archive: params.archive,
            }

            await this.baseClient.callTypedAPI(`/admin/users/${encodeURIComponent(params.userId)}/archive`, {method: "POST", body: JSON.stringify(body)})
        }

        public async adminCreateEmailTemplate(params: RequestType<typeof api_admin_email_comms_adminCreateEmailTemplate>): Promise<ResponseType<typeof api_admin_email_comms_adminCreateEmailTemplate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-templates`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminCreateEmailTemplate>
        }

        public async adminDeleteEmailTemplate(params: { id: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/email-templates/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        public async adminDeleteUser(params: { userId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/users/${encodeURIComponent(params.userId)}`, {method: "DELETE", body: undefined})
        }

        public async adminGetDocumentDownloadUrl(params: { documentId: string }): Promise<ResponseType<typeof api_admin_verify_document_adminGetDocumentDownloadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/documents/${encodeURIComponent(params.documentId)}/download-url`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_verify_document_adminGetDocumentDownloadUrl>
        }

        public async adminGetPlatformStats(): Promise<ResponseType<typeof api_admin_platform_adminGetPlatformStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_platform_adminGetPlatformStats>
        }

        public async adminGetReferenceCheck(params: { referenceId: string }): Promise<ResponseType<typeof api_admin_reference_check_adminGetReferenceCheck>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/references/${encodeURIComponent(params.referenceId)}/check`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_reference_check_adminGetReferenceCheck>
        }

        public async adminGetWorkerDocuments(params: { workerId: string }): Promise<ResponseType<typeof api_admin_workers_adminGetWorkerDocuments>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/workers/${encodeURIComponent(params.workerId)}/documents`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_workers_adminGetWorkerDocuments>
        }

        public async adminGetWorkerReferences(params: { workerId: string }): Promise<ResponseType<typeof api_admin_workers_adminGetWorkerReferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/workers/${encodeURIComponent(params.workerId)}/references`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_workers_adminGetWorkerReferences>
        }

        public async adminGrantSubscription(params: RequestType<typeof api_admin_employers_adminGrantSubscription>): Promise<ResponseType<typeof api_admin_employers_adminGrantSubscription>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                durationDays: params.durationDays,
                notes:        params.notes,
                plan:         params.plan,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/employers/${encodeURIComponent(params.employerId)}/grant-subscription`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_employers_adminGrantSubscription>
        }

        public async adminListAuditLog(params: RequestType<typeof api_admin_settings_adminListAuditLog>): Promise<ResponseType<typeof api_admin_settings_adminListAuditLog>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                action: params.action,
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/audit-log`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_settings_adminListAuditLog>
        }

        public async adminListEmailLog(params: RequestType<typeof api_admin_email_comms_adminListEmailLog>): Promise<ResponseType<typeof api_admin_email_comms_adminListEmailLog>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-comms/log`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminListEmailLog>
        }

        public async adminListEmailTemplates(): Promise<ResponseType<typeof api_admin_email_comms_adminListEmailTemplates>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-templates`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminListEmailTemplates>
        }

        public async adminListEmployers(): Promise<ResponseType<typeof api_admin_employers_adminListEmployers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/employers`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_employers_adminListEmployers>
        }

        public async adminListJobs(): Promise<ResponseType<typeof api_admin_platform_adminListJobs>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/jobs`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_platform_adminListJobs>
        }

        public async adminListSettings(): Promise<ResponseType<typeof api_admin_settings_adminListSettings>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/settings`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_settings_adminListSettings>
        }

        public async adminListUserEmailLog(params: RequestType<typeof api_admin_email_comms_adminListUserEmailLog>): Promise<ResponseType<typeof api_admin_email_comms_adminListUserEmailLog>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-comms/log/user/${encodeURIComponent(params.userId)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminListUserEmailLog>
        }

        public async adminListUsers(): Promise<ResponseType<typeof api_admin_platform_adminListUsers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_platform_adminListUsers>
        }

        public async adminListWorkers(): Promise<ResponseType<typeof api_admin_workers_adminListWorkers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/workers`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_workers_adminListWorkers>
        }

        public async adminResetUserPassword(params: { userId: string }): Promise<ResponseType<typeof api_admin_reset_user_password_adminResetUserPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users/${encodeURIComponent(params.userId)}/reset-password`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_reset_user_password_adminResetUserPassword>
        }

        public async adminRevokeSubscription(params: { employerId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/employers/${encodeURIComponent(params.employerId)}/revoke-subscription`, {method: "POST", body: undefined})
        }

        public async adminSendBulkEmail(params: RequestType<typeof api_admin_email_comms_adminSendBulkEmail>): Promise<ResponseType<typeof api_admin_email_comms_adminSendBulkEmail>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-comms/send-bulk`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminSendBulkEmail>
        }

        public async adminSendDocumentMessage(params: RequestType<typeof api_admin_document_message_adminSendDocumentMessage>): Promise<ResponseType<typeof api_admin_document_message_adminSendDocumentMessage>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                message: params.message,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/workers/${encodeURIComponent(params.workerId)}/documents/${encodeURIComponent(params.documentId)}/message`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_document_message_adminSendDocumentMessage>
        }

        public async adminSendEmailToUser(params: RequestType<typeof api_admin_email_comms_adminSendEmailToUser>): Promise<ResponseType<typeof api_admin_email_comms_adminSendEmailToUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-comms/send-to-user`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminSendEmailToUser>
        }

        public async adminSubmitReferenceCheck(params: RequestType<typeof api_admin_reference_check_adminSubmitReferenceCheck>): Promise<ResponseType<typeof api_admin_reference_check_adminSubmitReferenceCheck>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                additionalComments: params.additionalComments,
                capacity:           params.capacity,
                conductedBy:        params.conductedBy,
                developmentAreas:   params.developmentAreas,
                employmentDates:    params.employmentDates,
                reasonForLeaving:   params.reasonForLeaving,
                relationship:       params.relationship,
                scores:             params.scores,
                strengths:          params.strengths,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/references/${encodeURIComponent(params.referenceId)}/check`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_reference_check_adminSubmitReferenceCheck>
        }

        public async adminSuspendUser(params: RequestType<typeof api_admin_platform_adminSuspendUser>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                suspend: params.suspend,
            }

            await this.baseClient.callTypedAPI(`/admin/users/${encodeURIComponent(params.userId)}/suspend`, {method: "POST", body: JSON.stringify(body)})
        }

        public async adminUpdateEmailTemplate(params: RequestType<typeof api_admin_email_comms_adminUpdateEmailTemplate>): Promise<ResponseType<typeof api_admin_email_comms_adminUpdateEmailTemplate>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                category:    params.category,
                description: params.description,
                htmlBody:    params.htmlBody,
                name:        params.name,
                subject:     params.subject,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/email-templates/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_email_comms_adminUpdateEmailTemplate>
        }

        public async adminUpdateJobStatus(params: RequestType<typeof api_admin_platform_adminUpdateJobStatus>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                status: params.status,
            }

            await this.baseClient.callTypedAPI(`/admin/jobs/${encodeURIComponent(params.jobId)}/status`, {method: "POST", body: JSON.stringify(body)})
        }

        public async adminUpdateSetting(params: RequestType<typeof api_admin_settings_adminUpdateSetting>): Promise<ResponseType<typeof api_admin_settings_adminUpdateSetting>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                value: params.value,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/settings/${encodeURIComponent(params.key)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_settings_adminUpdateSetting>
        }

        public async adminVerifyDocument(params: RequestType<typeof api_admin_verify_document_adminVerifyDocument>): Promise<ResponseType<typeof api_admin_verify_document_adminVerifyDocument>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                action:          params.action,
                rejectionReason: params.rejectionReason,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/documents/${encodeURIComponent(params.documentId)}/verify`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_verify_document_adminVerifyDocument>
        }

        public async adminVerifyReference(params: RequestType<typeof api_admin_workers_adminVerifyReference>): Promise<ResponseType<typeof api_admin_workers_adminVerifyReference>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                status: params.status,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/references/${encodeURIComponent(params.referenceId)}/verify`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_workers_adminVerifyReference>
        }

        public async adminVerifyUserEmail(params: { userId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/users/${encodeURIComponent(params.userId)}/verify-email`, {method: "POST", body: undefined})
        }

        public async bootstrap(): Promise<ResponseType<typeof api_admin_bootstrap_bootstrap>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/bootstrap`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_bootstrap_bootstrap>
        }

        public async createComplianceOfficer(params: RequestType<typeof api_admin_compliance_officers_createComplianceOfficer>): Promise<ResponseType<typeof api_admin_compliance_officers_createComplianceOfficer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/compliance-officers`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_compliance_officers_createComplianceOfficer>
        }

        public async deleteComplianceOfficer(params: { userId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/compliance-officers/${encodeURIComponent(params.userId)}`, {method: "DELETE", body: undefined})
        }

        public async getPrivacyPolicy(): Promise<ResponseType<typeof api_admin_privacy_policy_getPrivacyPolicy>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/privacy-policy`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_privacy_policy_getPrivacyPolicy>
        }

        public async listComplianceOfficers(): Promise<ResponseType<typeof api_admin_compliance_officers_listComplianceOfficers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/compliance-officers`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_compliance_officers_listComplianceOfficers>
        }

        public async listDemoLeads(): Promise<ResponseType<typeof api_admin_demo_leads_listDemoLeads>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/demo-leads`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_demo_leads_listDemoLeads>
        }

        public async seed(): Promise<ResponseType<typeof api_admin_seed_seed>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/seed`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_seed_seed>
        }

        public async updatePrivacyPolicy(params: RequestType<typeof api_admin_privacy_policy_updatePrivacyPolicy>): Promise<void> {
            await this.baseClient.callTypedAPI(`/admin/privacy-policy`, {method: "POST", body: JSON.stringify(params)})
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { login as api_auth_login_login } from "~backend/auth/login";
import {
    register as api_auth_register_register,
    resendVerification as api_auth_register_resendVerification
} from "~backend/auth/register";
import {
    forgotPassword as api_auth_reset_password_forgotPassword,
    resetPassword as api_auth_reset_password_resetPassword
} from "~backend/auth/reset_password";
import { verifyEmail as api_auth_verifyEmail_verifyEmail } from "~backend/auth/verifyEmail";

export namespace auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.forgotPassword = this.forgotPassword.bind(this)
            this.login = this.login.bind(this)
            this.register = this.register.bind(this)
            this.resendVerification = this.resendVerification.bind(this)
            this.resetPassword = this.resetPassword.bind(this)
            this.verifyEmail = this.verifyEmail.bind(this)
        }

        public async forgotPassword(params: RequestType<typeof api_auth_reset_password_forgotPassword>): Promise<ResponseType<typeof api_auth_reset_password_forgotPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/forgot-password`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_reset_password_forgotPassword>
        }

        /**
         * Authenticates a user and returns a JWT token.
         */
        public async login(params: RequestType<typeof api_auth_login_login>): Promise<ResponseType<typeof api_auth_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_login_login>
        }

        public async register(params: RequestType<typeof api_auth_register_register>): Promise<ResponseType<typeof api_auth_register_register>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/register`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_register_register>
        }

        public async resendVerification(params: RequestType<typeof api_auth_register_resendVerification>): Promise<ResponseType<typeof api_auth_register_resendVerification>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/resend-verification`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_register_resendVerification>
        }

        public async resetPassword(params: RequestType<typeof api_auth_reset_password_resetPassword>): Promise<ResponseType<typeof api_auth_reset_password_resetPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/reset-password`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_reset_password_resetPassword>
        }

        public async verifyEmail(params: RequestType<typeof api_auth_verifyEmail_verifyEmail>): Promise<ResponseType<typeof api_auth_verifyEmail_verifyEmail>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/verify-email`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_verifyEmail_verifyEmail>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { getMapsKey as api_config_maps_key_getMapsKey } from "~backend/config/maps_key";

export namespace config {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getMapsKey = this.getMapsKey.bind(this)
        }

        public async getMapsKey(): Promise<ResponseType<typeof api_config_maps_key_getMapsKey>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/config/maps-key`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_config_maps_key_getMapsKey>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { listDemoPairs as api_demo_list_pairs_listDemoPairs } from "~backend/demo/list_pairs";
import { demoLogin as api_demo_login_demoLogin } from "~backend/demo/login";
import { registerDemoLead as api_demo_register_lead_registerDemoLead } from "~backend/demo/register_lead";
import { seedDemo as api_demo_seed_seedDemo } from "~backend/demo/seed";

export namespace demo {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.demoLogin = this.demoLogin.bind(this)
            this.listDemoPairs = this.listDemoPairs.bind(this)
            this.registerDemoLead = this.registerDemoLead.bind(this)
            this.seedDemo = this.seedDemo.bind(this)
        }

        public async demoLogin(params: RequestType<typeof api_demo_login_demoLogin>): Promise<ResponseType<typeof api_demo_login_demoLogin>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/demo/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_demo_login_demoLogin>
        }

        public async listDemoPairs(): Promise<ResponseType<typeof api_demo_list_pairs_listDemoPairs>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/demo/pairs`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_demo_list_pairs_listDemoPairs>
        }

        public async registerDemoLead(params: RequestType<typeof api_demo_register_lead_registerDemoLead>): Promise<ResponseType<typeof api_demo_register_lead_registerDemoLead>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/demo/leads`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_demo_register_lead_registerDemoLead>
        }

        public async seedDemo(): Promise<ResponseType<typeof api_demo_seed_seedDemo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/demo/seed`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_demo_seed_seedDemo>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    confirmLogoUpload as api_employers_logo_confirmLogoUpload,
    getLogoUploadUrl as api_employers_logo_getLogoUploadUrl
} from "~backend/employers/logo";
import { getEmployerProfile as api_employers_profile_get_getEmployerProfile } from "~backend/employers/profile_get";
import { updateEmployerProfile as api_employers_profile_update_updateEmployerProfile } from "~backend/employers/profile_update";

export namespace employers {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.confirmLogoUpload = this.confirmLogoUpload.bind(this)
            this.deleteEmployerLogo = this.deleteEmployerLogo.bind(this)
            this.getEmployerProfile = this.getEmployerProfile.bind(this)
            this.getLogoUploadUrl = this.getLogoUploadUrl.bind(this)
            this.updateEmployerProfile = this.updateEmployerProfile.bind(this)
        }

        public async confirmLogoUpload(params: RequestType<typeof api_employers_logo_confirmLogoUpload>): Promise<ResponseType<typeof api_employers_logo_confirmLogoUpload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/logo/confirm`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_employers_logo_confirmLogoUpload>
        }

        public async deleteEmployerLogo(): Promise<void> {
            await this.baseClient.callTypedAPI(`/employers/logo`, {method: "DELETE", body: undefined})
        }

        public async getEmployerProfile(): Promise<ResponseType<typeof api_employers_profile_get_getEmployerProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/profile`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_employers_profile_get_getEmployerProfile>
        }

        public async getLogoUploadUrl(params: RequestType<typeof api_employers_logo_getLogoUploadUrl>): Promise<ResponseType<typeof api_employers_logo_getLogoUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/logo/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_employers_logo_getLogoUploadUrl>
        }

        public async updateEmployerProfile(params: RequestType<typeof api_employers_profile_update_updateEmployerProfile>): Promise<ResponseType<typeof api_employers_profile_update_updateEmployerProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/profile`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_employers_profile_update_updateEmployerProfile>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { bulkSyncToGHL as api_ghl_bulk_sync_bulkSyncToGHL } from "~backend/ghl/bulk_sync";

export namespace ghl {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.bulkSyncToGHL = this.bulkSyncToGHL.bind(this)
        }

        public async bulkSyncToGHL(): Promise<ResponseType<typeof api_ghl_bulk_sync_bulkSyncToGHL>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/ghl/bulk-sync`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_ghl_bulk_sync_bulkSyncToGHL>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { cancelJobRequest as api_jobs_cancel_cancelJobRequest } from "~backend/jobs/cancel";
import { createJobRequest as api_jobs_create_createJobRequest } from "~backend/jobs/create";
import {
    expressInterest as api_jobs_emergency_respond_expressInterest,
    listEmergencyResponses as api_jobs_emergency_respond_listEmergencyResponses
} from "~backend/jobs/emergency_respond";
import {
    expressJobInterest as api_jobs_express_interest_expressJobInterest,
    listJobInterest as api_jobs_express_interest_listJobInterest
} from "~backend/jobs/express_interest";
import { getJobRequest as api_jobs_get_getJobRequest } from "~backend/jobs/get";
import { listJobRequests as api_jobs_list_listJobRequests } from "~backend/jobs/list";
import { listEmergencyJobs as api_jobs_list_emergency_listEmergencyJobs } from "~backend/jobs/list_emergency";
import { getPublicJob as api_jobs_public_get_getPublicJob } from "~backend/jobs/public_get";
import { listSupportTypeTags as api_jobs_tags_listSupportTypeTags } from "~backend/jobs/tags";
import { updateJobRequest as api_jobs_update_updateJobRequest } from "~backend/jobs/update";

export namespace jobs {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.cancelJobRequest = this.cancelJobRequest.bind(this)
            this.createJobRequest = this.createJobRequest.bind(this)
            this.expressInterest = this.expressInterest.bind(this)
            this.expressJobInterest = this.expressJobInterest.bind(this)
            this.getJobRequest = this.getJobRequest.bind(this)
            this.getPublicJob = this.getPublicJob.bind(this)
            this.listEmergencyJobs = this.listEmergencyJobs.bind(this)
            this.listEmergencyResponses = this.listEmergencyResponses.bind(this)
            this.listJobInterest = this.listJobInterest.bind(this)
            this.listJobRequests = this.listJobRequests.bind(this)
            this.listSupportTypeTags = this.listSupportTypeTags.bind(this)
            this.updateJobRequest = this.updateJobRequest.bind(this)
        }

        public async cancelJobRequest(params: { jobId: string }): Promise<ResponseType<typeof api_jobs_cancel_cancelJobRequest>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}/cancel`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_cancel_cancelJobRequest>
        }

        public async createJobRequest(params: RequestType<typeof api_jobs_create_createJobRequest>): Promise<ResponseType<typeof api_jobs_create_createJobRequest>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_create_createJobRequest>
        }

        public async expressInterest(params: RequestType<typeof api_jobs_emergency_respond_expressInterest>): Promise<ResponseType<typeof api_jobs_emergency_respond_expressInterest>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                note: params.note,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}/interest`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_emergency_respond_expressInterest>
        }

        public async expressJobInterest(params: RequestType<typeof api_jobs_express_interest_expressJobInterest>): Promise<ResponseType<typeof api_jobs_express_interest_expressJobInterest>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                note: params.note,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}/express-interest`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_express_interest_expressJobInterest>
        }

        public async getJobRequest(params: { jobId: string }): Promise<ResponseType<typeof api_jobs_get_getJobRequest>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_get_getJobRequest>
        }

        public async getPublicJob(params: { jobId: string }): Promise<ResponseType<typeof api_jobs_public_get_getPublicJob>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/public/${encodeURIComponent(params.jobId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_public_get_getPublicJob>
        }

        public async listEmergencyJobs(): Promise<ResponseType<typeof api_jobs_list_emergency_listEmergencyJobs>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/emergency`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_list_emergency_listEmergencyJobs>
        }

        public async listEmergencyResponses(params: { jobId: string }): Promise<ResponseType<typeof api_jobs_emergency_respond_listEmergencyResponses>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}/interest`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_emergency_respond_listEmergencyResponses>
        }

        public async listJobInterest(params: { jobId: string }): Promise<ResponseType<typeof api_jobs_express_interest_listJobInterest>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}/express-interest`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_express_interest_listJobInterest>
        }

        public async listJobRequests(): Promise<ResponseType<typeof api_jobs_list_listJobRequests>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_list_listJobRequests>
        }

        public async listSupportTypeTags(): Promise<ResponseType<typeof api_jobs_tags_listSupportTypeTags>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/support-type-tags`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_tags_listSupportTypeTags>
        }

        public async updateJobRequest(params: RequestType<typeof api_jobs_update_updateJobRequest>): Promise<ResponseType<typeof api_jobs_update_updateJobRequest>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                ageRangePreference:        params.ageRangePreference,
                behaviouralConsiderations: params.behaviouralConsiderations,
                clientNotes:               params.clientNotes,
                genderPreference:          params.genderPreference,
                isEmergency:               params.isEmergency,
                jobTitle:                  params.jobTitle,
                latitude:                  params.latitude,
                location:                  params.location,
                longitude:                 params.longitude,
                medicalRequirements:       params.medicalRequirements,
                publicHolidayRate:         params.publicHolidayRate,
                responseDeadline:          params.responseDeadline,
                shiftDate:                 params.shiftDate,
                shiftDurationHours:        params.shiftDurationHours,
                shiftStartTime:            params.shiftStartTime,
                status:                    params.status,
                supportTypeTags:           params.supportTypeTags,
                weekdayRate:               params.weekdayRate,
                weekendRate:               params.weekendRate,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/jobs/${encodeURIComponent(params.jobId)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_jobs_update_updateJobRequest>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { matchJobsForWorker as api_matching_match_jobs_matchJobsForWorker } from "~backend/matching/match_jobs";
import { matchWorkersForJob as api_matching_match_workers_matchWorkersForJob } from "~backend/matching/match_workers";

export namespace matching {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.matchJobsForWorker = this.matchJobsForWorker.bind(this)
            this.matchWorkersForJob = this.matchWorkersForJob.bind(this)
        }

        public async matchJobsForWorker(): Promise<ResponseType<typeof api_matching_match_jobs_matchJobsForWorker>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/matching/workers/jobs`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_matching_match_jobs_matchJobsForWorker>
        }

        public async matchWorkersForJob(params: RequestType<typeof api_matching_match_workers_matchWorkersForJob>): Promise<ResponseType<typeof api_matching_match_workers_matchWorkersForJob>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:          params.limit === undefined ? undefined : String(params.limit),
                maxDistanceKm:  params.maxDistanceKm === undefined ? undefined : String(params.maxDistanceKm),
                requiredSkills: params.requiredSkills?.map((v) => v),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/matching/jobs/${encodeURIComponent(params.jobId)}/workers`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_matching_match_workers_matchWorkersForJob>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    listMessages as api_messages_messages_listMessages,
    sendMessage as api_messages_messages_sendMessage
} from "~backend/messages/messages";

export namespace messages {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.listMessages = this.listMessages.bind(this)
            this.sendMessage = this.sendMessage.bind(this)
        }

        public async listMessages(params: { offerId: string }): Promise<ResponseType<typeof api_messages_messages_listMessages>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/messages`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_messages_messages_listMessages>
        }

        public async sendMessage(params: RequestType<typeof api_messages_messages_sendMessage>): Promise<ResponseType<typeof api_messages_messages_sendMessage>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                body: params.body,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/messages`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_messages_messages_sendMessage>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    listNotifications as api_notifications_list_listNotifications,
    markNotificationsRead as api_notifications_list_markNotificationsRead
} from "~backend/notifications/list";

export namespace notifications {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.listNotifications = this.listNotifications.bind(this)
            this.markNotificationsRead = this.markNotificationsRead.bind(this)
        }

        public async listNotifications(): Promise<ResponseType<typeof api_notifications_list_listNotifications>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_list_listNotifications>
        }

        public async markNotificationsRead(params: RequestType<typeof api_notifications_list_markNotificationsRead>): Promise<void> {
            await this.baseClient.callTypedAPI(`/notifications/read`, {method: "POST", body: JSON.stringify(params)})
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { createOffer as api_offers_create_createOffer } from "~backend/offers/create";
import { getOffer as api_offers_get_getOffer } from "~backend/offers/get";
import { listOffers as api_offers_list_listOffers } from "~backend/offers/list";
import { employerNegotiate as api_offers_negotiate_employerNegotiate } from "~backend/offers/negotiate";
import { workerRespond as api_offers_respond_workerRespond } from "~backend/offers/respond";

export namespace offers {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createOffer = this.createOffer.bind(this)
            this.employerNegotiate = this.employerNegotiate.bind(this)
            this.getOffer = this.getOffer.bind(this)
            this.listOffers = this.listOffers.bind(this)
            this.workerRespond = this.workerRespond.bind(this)
        }

        public async createOffer(params: RequestType<typeof api_offers_create_createOffer>): Promise<ResponseType<typeof api_offers_create_createOffer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_offers_create_createOffer>
        }

        public async employerNegotiate(params: RequestType<typeof api_offers_negotiate_employerNegotiate>): Promise<ResponseType<typeof api_offers_negotiate_employerNegotiate>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                action:      params.action,
                counterRate: params.counterRate,
                note:        params.note,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/negotiate`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_offers_negotiate_employerNegotiate>
        }

        public async getOffer(params: { offerId: string }): Promise<ResponseType<typeof api_offers_get_getOffer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_offers_get_getOffer>
        }

        public async listOffers(params: RequestType<typeof api_offers_list_listOffers>): Promise<ResponseType<typeof api_offers_list_listOffers>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                status: params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_offers_list_listOffers>
        }

        public async workerRespond(params: RequestType<typeof api_offers_respond_workerRespond>): Promise<ResponseType<typeof api_offers_respond_workerRespond>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                action:       params.action,
                note:         params.note,
                proposedRate: params.proposedRate,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/respond`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_offers_respond_workerRespond>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { createCheckoutSession as api_payments_checkout_createCheckoutSession } from "~backend/payments/checkout";
import { createEmployerCheckoutSession as api_payments_employer_checkout_createEmployerCheckoutSession } from "~backend/payments/employer_checkout";
import { getEmployerSubscriptionStatus as api_payments_employer_status_getEmployerSubscriptionStatus } from "~backend/payments/employer_status";
import { getPaymentStatus as api_payments_status_getPaymentStatus } from "~backend/payments/status";
import { testActivateEmployerSubscription as api_payments_test_activate_testActivateEmployerSubscription } from "~backend/payments/test_activate";

export namespace payments {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createCheckoutSession = this.createCheckoutSession.bind(this)
            this.createEmployerCheckoutSession = this.createEmployerCheckoutSession.bind(this)
            this.getEmployerSubscriptionStatus = this.getEmployerSubscriptionStatus.bind(this)
            this.getPaymentStatus = this.getPaymentStatus.bind(this)
            this.stripeWebhook = this.stripeWebhook.bind(this)
            this.testActivateEmployerSubscription = this.testActivateEmployerSubscription.bind(this)
        }

        public async createCheckoutSession(params: RequestType<typeof api_payments_checkout_createCheckoutSession>): Promise<ResponseType<typeof api_payments_checkout_createCheckoutSession>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/payments/checkout`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_payments_checkout_createCheckoutSession>
        }

        public async createEmployerCheckoutSession(params: RequestType<typeof api_payments_employer_checkout_createEmployerCheckoutSession>): Promise<ResponseType<typeof api_payments_employer_checkout_createEmployerCheckoutSession>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/payments/employer/checkout`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_payments_employer_checkout_createEmployerCheckoutSession>
        }

        public async getEmployerSubscriptionStatus(): Promise<ResponseType<typeof api_payments_employer_status_getEmployerSubscriptionStatus>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/payments/employer/status`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_payments_employer_status_getEmployerSubscriptionStatus>
        }

        public async getPaymentStatus(): Promise<ResponseType<typeof api_payments_status_getPaymentStatus>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/payments/status`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_payments_status_getPaymentStatus>
        }

        public async stripeWebhook(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/payments/webhook`, options)
        }

        public async testActivateEmployerSubscription(params: RequestType<typeof api_payments_test_activate_testActivateEmployerSubscription>): Promise<ResponseType<typeof api_payments_test_activate_testActivateEmployerSubscription>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/payments/employer/test-activate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_payments_test_activate_testActivateEmployerSubscription>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getMyReviewForOffer as api_reviews_reviews_getMyReviewForOffer,
    getReviews as api_reviews_reviews_getReviews,
    submitReview as api_reviews_reviews_submitReview
} from "~backend/reviews/reviews";

export namespace reviews {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getMyReviewForOffer = this.getMyReviewForOffer.bind(this)
            this.getReviews = this.getReviews.bind(this)
            this.submitReview = this.submitReview.bind(this)
        }

        public async getMyReviewForOffer(params: { offerId: string }): Promise<ResponseType<typeof api_reviews_reviews_getMyReviewForOffer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/review`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_reviews_reviews_getMyReviewForOffer>
        }

        public async getReviews(params: { userId: string }): Promise<ResponseType<typeof api_reviews_reviews_getReviews>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/reviews/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_reviews_reviews_getReviews>
        }

        public async submitReview(params: RequestType<typeof api_reviews_reviews_submitReview>): Promise<ResponseType<typeof api_reviews_reviews_submitReview>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                comment: params.comment,
                rating:  params.rating,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/offers/${encodeURIComponent(params.offerId)}/review`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_reviews_reviews_submitReview>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getAccountDetail as api_sales_account_edit_getAccountDetail,
    salesActivateSubscription as api_sales_account_edit_salesActivateSubscription,
    salesCancelSubscription as api_sales_account_edit_salesCancelSubscription,
    salesExtendSubscription as api_sales_account_edit_salesExtendSubscription,
    salesForceVerifyEmail as api_sales_account_edit_salesForceVerifyEmail,
    salesGrantWorkerPackage as api_sales_account_edit_salesGrantWorkerPackage,
    salesUpdateEmployer as api_sales_account_edit_salesUpdateEmployer,
    salesUpdateWorker as api_sales_account_edit_salesUpdateWorker
} from "~backend/sales/account_edit";
import {
    addAccountNote as api_sales_accounts_addAccountNote,
    deleteAccountNote as api_sales_accounts_deleteAccountNote,
    listAccounts as api_sales_accounts_listAccounts
} from "~backend/sales/accounts";
import {
    createSalesAgent as api_sales_agents_createSalesAgent,
    deleteSalesAgent as api_sales_agents_deleteSalesAgent,
    listSalesAgents as api_sales_agents_listSalesAgents
} from "~backend/sales/agents";
import { getSalesDashboard as api_sales_dashboard_getSalesDashboard } from "~backend/sales/dashboard";
import {
    createDemo as api_sales_demos_createDemo,
    deleteDemo as api_sales_demos_deleteDemo,
    listDemos as api_sales_demos_listDemos,
    updateDemoStatus as api_sales_demos_updateDemoStatus
} from "~backend/sales/demos";
import {
    createDiscount as api_sales_discounts_createDiscount,
    deleteDiscount as api_sales_discounts_deleteDiscount,
    listDiscounts as api_sales_discounts_listDiscounts,
    toggleDiscount as api_sales_discounts_toggleDiscount
} from "~backend/sales/discounts";
import { listEmployerJobs as api_sales_list_employer_jobs_listEmployerJobs } from "~backend/sales/list_employer_jobs";
import { redeemPromo as api_sales_redeem_promo_redeemPromo } from "~backend/sales/redeem_promo";
import { validatePromo as api_sales_validate_promo_validatePromo } from "~backend/sales/validate_promo";

export namespace sales {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addAccountNote = this.addAccountNote.bind(this)
            this.createDemo = this.createDemo.bind(this)
            this.createDiscount = this.createDiscount.bind(this)
            this.createSalesAgent = this.createSalesAgent.bind(this)
            this.deleteAccountNote = this.deleteAccountNote.bind(this)
            this.deleteDemo = this.deleteDemo.bind(this)
            this.deleteDiscount = this.deleteDiscount.bind(this)
            this.deleteSalesAgent = this.deleteSalesAgent.bind(this)
            this.getAccountDetail = this.getAccountDetail.bind(this)
            this.getSalesDashboard = this.getSalesDashboard.bind(this)
            this.listAccounts = this.listAccounts.bind(this)
            this.listDemos = this.listDemos.bind(this)
            this.listDiscounts = this.listDiscounts.bind(this)
            this.listEmployerJobs = this.listEmployerJobs.bind(this)
            this.listSalesAgents = this.listSalesAgents.bind(this)
            this.redeemPromo = this.redeemPromo.bind(this)
            this.salesActivateSubscription = this.salesActivateSubscription.bind(this)
            this.salesCancelSubscription = this.salesCancelSubscription.bind(this)
            this.salesExtendSubscription = this.salesExtendSubscription.bind(this)
            this.salesForceVerifyEmail = this.salesForceVerifyEmail.bind(this)
            this.salesGrantWorkerPackage = this.salesGrantWorkerPackage.bind(this)
            this.salesUpdateEmployer = this.salesUpdateEmployer.bind(this)
            this.salesUpdateWorker = this.salesUpdateWorker.bind(this)
            this.toggleDiscount = this.toggleDiscount.bind(this)
            this.updateDemoStatus = this.updateDemoStatus.bind(this)
            this.validatePromo = this.validatePromo.bind(this)
        }

        public async addAccountNote(params: RequestType<typeof api_sales_accounts_addAccountNote>): Promise<ResponseType<typeof api_sales_accounts_addAccountNote>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                note: params.note,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/notes`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_accounts_addAccountNote>
        }

        public async createDemo(params: RequestType<typeof api_sales_demos_createDemo>): Promise<ResponseType<typeof api_sales_demos_createDemo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/demos`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_demos_createDemo>
        }

        public async createDiscount(params: RequestType<typeof api_sales_discounts_createDiscount>): Promise<ResponseType<typeof api_sales_discounts_createDiscount>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/discounts`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_discounts_createDiscount>
        }

        public async createSalesAgent(params: RequestType<typeof api_sales_agents_createSalesAgent>): Promise<ResponseType<typeof api_sales_agents_createSalesAgent>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/agents`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_agents_createSalesAgent>
        }

        public async deleteAccountNote(params: { noteId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/sales/notes/${encodeURIComponent(params.noteId)}`, {method: "DELETE", body: undefined})
        }

        public async deleteDemo(params: { demoId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/sales/demos/${encodeURIComponent(params.demoId)}`, {method: "DELETE", body: undefined})
        }

        public async deleteDiscount(params: { discountId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/sales/discounts/${encodeURIComponent(params.discountId)}`, {method: "DELETE", body: undefined})
        }

        public async deleteSalesAgent(params: { userId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/sales/agents/${encodeURIComponent(params.userId)}`, {method: "DELETE", body: undefined})
        }

        public async getAccountDetail(params: { userId: string }): Promise<ResponseType<typeof api_sales_account_edit_getAccountDetail>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/detail`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_getAccountDetail>
        }

        public async getSalesDashboard(): Promise<ResponseType<typeof api_sales_dashboard_getSalesDashboard>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/dashboard`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_dashboard_getSalesDashboard>
        }

        public async listAccounts(): Promise<ResponseType<typeof api_sales_accounts_listAccounts>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_accounts_listAccounts>
        }

        public async listDemos(): Promise<ResponseType<typeof api_sales_demos_listDemos>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/demos`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_demos_listDemos>
        }

        public async listDiscounts(): Promise<ResponseType<typeof api_sales_discounts_listDiscounts>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/discounts`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_discounts_listDiscounts>
        }

        public async listEmployerJobs(params: { userId: string }): Promise<ResponseType<typeof api_sales_list_employer_jobs_listEmployerJobs>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/jobs`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_list_employer_jobs_listEmployerJobs>
        }

        public async listSalesAgents(): Promise<ResponseType<typeof api_sales_agents_listSalesAgents>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/agents`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_agents_listSalesAgents>
        }

        public async redeemPromo(params: RequestType<typeof api_sales_redeem_promo_redeemPromo>): Promise<ResponseType<typeof api_sales_redeem_promo_redeemPromo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/discounts/redeem`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_redeem_promo_redeemPromo>
        }

        public async salesActivateSubscription(params: RequestType<typeof api_sales_account_edit_salesActivateSubscription>): Promise<ResponseType<typeof api_sales_account_edit_salesActivateSubscription>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                amountAudCents: params.amountAudCents,
                periodDays:     params.periodDays,
                plan:           params.plan,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/activate-subscription`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesActivateSubscription>
        }

        public async salesCancelSubscription(params: { userId: string }): Promise<ResponseType<typeof api_sales_account_edit_salesCancelSubscription>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/cancel-subscription`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesCancelSubscription>
        }

        public async salesExtendSubscription(params: RequestType<typeof api_sales_account_edit_salesExtendSubscription>): Promise<ResponseType<typeof api_sales_account_edit_salesExtendSubscription>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                additionalDays: params.additionalDays,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/extend-subscription`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesExtendSubscription>
        }

        public async salesForceVerifyEmail(params: { userId: string }): Promise<ResponseType<typeof api_sales_account_edit_salesForceVerifyEmail>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/verify-email`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesForceVerifyEmail>
        }

        public async salesGrantWorkerPackage(params: RequestType<typeof api_sales_account_edit_salesGrantWorkerPackage>): Promise<ResponseType<typeof api_sales_account_edit_salesGrantWorkerPackage>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                grantDocs:          params.grantDocs,
                grantPriorityBoost: params.grantPriorityBoost,
                grantRefs:          params.grantRefs,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/grant-worker-package`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesGrantWorkerPackage>
        }

        public async salesUpdateEmployer(params: RequestType<typeof api_sales_account_edit_salesUpdateEmployer>): Promise<ResponseType<typeof api_sales_account_edit_salesUpdateEmployer>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                contactPerson:    params.contactPerson,
                email:            params.email,
                location:         params.location,
                organisationName: params.organisationName,
                phone:            params.phone,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/update-employer`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesUpdateEmployer>
        }

        public async salesUpdateWorker(params: RequestType<typeof api_sales_account_edit_salesUpdateWorker>): Promise<ResponseType<typeof api_sales_account_edit_salesUpdateWorker>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                bio:             params.bio,
                experienceYears: params.experienceYears,
                location:        params.location,
                name:            params.name,
                phone:           params.phone,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/accounts/${encodeURIComponent(params.userId)}/update-worker`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_account_edit_salesUpdateWorker>
        }

        public async toggleDiscount(params: { discountId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/sales/discounts/${encodeURIComponent(params.discountId)}/toggle`, {method: "POST", body: undefined})
        }

        public async updateDemoStatus(params: RequestType<typeof api_sales_demos_updateDemoStatus>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                outcome: params.outcome,
                status:  params.status,
            }

            await this.baseClient.callTypedAPI(`/sales/demos/${encodeURIComponent(params.demoId)}/status`, {method: "POST", body: JSON.stringify(body)})
        }

        public async validatePromo(params: RequestType<typeof api_sales_validate_promo_validatePromo>): Promise<ResponseType<typeof api_sales_validate_promo_validatePromo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/sales/discounts/validate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_sales_validate_promo_validatePromo>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    adminListSupportTickets as api_support_list_tickets_adminListSupportTickets,
    adminUpdateSupportTicket as api_support_list_tickets_adminUpdateSupportTicket
} from "~backend/support/list_tickets";
import { submitSupportTicket as api_support_submit_submitSupportTicket } from "~backend/support/submit";

export namespace support {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.adminListSupportTickets = this.adminListSupportTickets.bind(this)
            this.adminUpdateSupportTicket = this.adminUpdateSupportTicket.bind(this)
            this.submitSupportTicket = this.submitSupportTicket.bind(this)
        }

        public async adminListSupportTickets(params: RequestType<typeof api_support_list_tickets_adminListSupportTickets>): Promise<ResponseType<typeof api_support_list_tickets_adminListSupportTickets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
                status: params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/support/tickets`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_support_list_tickets_adminListSupportTickets>
        }

        public async adminUpdateSupportTicket(params: RequestType<typeof api_support_list_tickets_adminUpdateSupportTicket>): Promise<ResponseType<typeof api_support_list_tickets_adminUpdateSupportTicket>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                adminNotes: params.adminNotes,
                status:     params.status,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/support/tickets/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_support_list_tickets_adminUpdateSupportTicket>
        }

        public async submitSupportTicket(params: RequestType<typeof api_support_submit_submitSupportTicket>): Promise<ResponseType<typeof api_support_submit_submitSupportTicket>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/support/tickets`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_support_submit_submitSupportTicket>
        }
    }
}


export namespace uploads {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.uploadEmailImage = this.uploadEmailImage.bind(this)
            this.uploadEmployerLogo = this.uploadEmployerLogo.bind(this)
            this.uploadWorkerAvatar = this.uploadWorkerAvatar.bind(this)
            this.uploadWorkerDocument = this.uploadWorkerDocument.bind(this)
            this.uploadWorkerResume = this.uploadWorkerResume.bind(this)
            this.uploadWorkerVideo = this.uploadWorkerVideo.bind(this)
        }

        public async uploadEmailImage(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/email-image`, options)
        }

        public async uploadEmployerLogo(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/employer-logo`, options)
        }

        public async uploadWorkerAvatar(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/worker-avatar`, options)
        }

        public async uploadWorkerDocument(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/worker-document`, options)
        }

        public async uploadWorkerResume(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/worker-resume`, options)
        }

        public async uploadWorkerVideo(options: PickMethods<"POST"> = {}): Promise<globalThis.Response> {
            options.method ||= "POST";
            return this.baseClient.callAPI(`/uploads/worker-video`, options)
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { me as api_users_me_me } from "~backend/users/me";

export namespace users {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.me = this.me.bind(this)
        }

        public async me(): Promise<ResponseType<typeof api_users_me_me>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/me`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_users_me_me>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getWorkerAvailability as api_workers_availability_getWorkerAvailability,
    upsertWorkerAvailability as api_workers_availability_upsertWorkerAvailability
} from "~backend/workers/availability";
import {
    confirmAvatarUpload as api_workers_avatar_confirmAvatarUpload,
    getAvatarUploadUrl as api_workers_avatar_getAvatarUploadUrl
} from "~backend/workers/avatar";
import { browseWorkers as api_workers_browse_browseWorkers } from "~backend/workers/browse";
import { getWorkerCompletion as api_workers_completion_getWorkerCompletion } from "~backend/workers/completion";
import {
    confirmDocumentUpload as api_workers_documents_confirmDocumentUpload,
    deleteWorkerDocument as api_workers_documents_deleteWorkerDocument,
    getDocumentUploadUrl as api_workers_documents_getDocumentUploadUrl,
    listDocumentTypes as api_workers_documents_listDocumentTypes,
    listWorkerDocuments as api_workers_documents_listWorkerDocuments,
    updateDocumentExpiry as api_workers_documents_updateDocumentExpiry
} from "~backend/workers/documents";
import {
    getDocumentDownloadUrl as api_workers_employer_documents_getDocumentDownloadUrl,
    listWorkerDocumentsForEmployer as api_workers_employer_documents_listWorkerDocumentsForEmployer
} from "~backend/workers/employer_documents";
import { getWorkerProfile as api_workers_profile_get_getWorkerProfile } from "~backend/workers/profile_get";
import { updateWorkerProfile as api_workers_profile_update_updateWorkerProfile } from "~backend/workers/profile_update";
import {
    createWorkerReference as api_workers_references_createWorkerReference,
    deleteWorkerReference as api_workers_references_deleteWorkerReference,
    getReferencesForEmployer as api_workers_references_getReferencesForEmployer,
    listWorkerReferences as api_workers_references_listWorkerReferences,
    updateWorkerReference as api_workers_references_updateWorkerReference
} from "~backend/workers/references";
import {
    confirmResumeUpload as api_workers_resume_confirmResumeUpload,
    getResumeUploadUrl as api_workers_resume_getResumeUploadUrl,
    getWorkerResume as api_workers_resume_getWorkerResume
} from "~backend/workers/resume";
import {
    getWorkerSkills as api_workers_skills_getWorkerSkills,
    listSkillTags as api_workers_skills_listSkillTags,
    updateWorkerSkills as api_workers_skills_updateWorkerSkills
} from "~backend/workers/skills";
import { getVerificationScore as api_workers_verification_score_getVerificationScore } from "~backend/workers/verification_score";
import {
    confirmVideoUpload as api_workers_video_confirmVideoUpload,
    getVideoUploadUrl as api_workers_video_getVideoUploadUrl,
    getWorkerVideo as api_workers_video_getWorkerVideo
} from "~backend/workers/video";

export namespace workers {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.browseWorkers = this.browseWorkers.bind(this)
            this.confirmAvatarUpload = this.confirmAvatarUpload.bind(this)
            this.confirmDocumentUpload = this.confirmDocumentUpload.bind(this)
            this.confirmResumeUpload = this.confirmResumeUpload.bind(this)
            this.confirmVideoUpload = this.confirmVideoUpload.bind(this)
            this.createWorkerReference = this.createWorkerReference.bind(this)
            this.deleteWorkerAvatar = this.deleteWorkerAvatar.bind(this)
            this.deleteWorkerDocument = this.deleteWorkerDocument.bind(this)
            this.deleteWorkerReference = this.deleteWorkerReference.bind(this)
            this.deleteWorkerResume = this.deleteWorkerResume.bind(this)
            this.deleteWorkerVideo = this.deleteWorkerVideo.bind(this)
            this.downloadWorkerDocumentsZip = this.downloadWorkerDocumentsZip.bind(this)
            this.getAvatarUploadUrl = this.getAvatarUploadUrl.bind(this)
            this.getDocumentDownloadUrl = this.getDocumentDownloadUrl.bind(this)
            this.getDocumentUploadUrl = this.getDocumentUploadUrl.bind(this)
            this.getReferencesForEmployer = this.getReferencesForEmployer.bind(this)
            this.getResumeUploadUrl = this.getResumeUploadUrl.bind(this)
            this.getVerificationScore = this.getVerificationScore.bind(this)
            this.getVideoUploadUrl = this.getVideoUploadUrl.bind(this)
            this.getWorkerAvailability = this.getWorkerAvailability.bind(this)
            this.getWorkerCompletion = this.getWorkerCompletion.bind(this)
            this.getWorkerProfile = this.getWorkerProfile.bind(this)
            this.getWorkerResume = this.getWorkerResume.bind(this)
            this.getWorkerSkills = this.getWorkerSkills.bind(this)
            this.getWorkerVideo = this.getWorkerVideo.bind(this)
            this.listDocumentTypes = this.listDocumentTypes.bind(this)
            this.listSkillTags = this.listSkillTags.bind(this)
            this.listWorkerDocuments = this.listWorkerDocuments.bind(this)
            this.listWorkerDocumentsForEmployer = this.listWorkerDocumentsForEmployer.bind(this)
            this.listWorkerReferences = this.listWorkerReferences.bind(this)
            this.updateDocumentExpiry = this.updateDocumentExpiry.bind(this)
            this.updateWorkerProfile = this.updateWorkerProfile.bind(this)
            this.updateWorkerReference = this.updateWorkerReference.bind(this)
            this.updateWorkerSkills = this.updateWorkerSkills.bind(this)
            this.upsertWorkerAvailability = this.upsertWorkerAvailability.bind(this)
        }

        public async browseWorkers(params: RequestType<typeof api_workers_browse_browseWorkers>): Promise<ResponseType<typeof api_workers_browse_browseWorkers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/browse`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_browse_browseWorkers>
        }

        public async confirmAvatarUpload(params: RequestType<typeof api_workers_avatar_confirmAvatarUpload>): Promise<ResponseType<typeof api_workers_avatar_confirmAvatarUpload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/avatar/confirm`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_avatar_confirmAvatarUpload>
        }

        /**
         * Confirms a compliance document upload and records it in the database.
         */
        public async confirmDocumentUpload(params: RequestType<typeof api_workers_documents_confirmDocumentUpload>): Promise<ResponseType<typeof api_workers_documents_confirmDocumentUpload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/documents/confirm`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_documents_confirmDocumentUpload>
        }

        public async confirmResumeUpload(params: RequestType<typeof api_workers_resume_confirmResumeUpload>): Promise<ResponseType<typeof api_workers_resume_confirmResumeUpload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/resume/confirm`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_resume_confirmResumeUpload>
        }

        /**
         * Confirms and saves the intro video after upload.
         */
        public async confirmVideoUpload(params: RequestType<typeof api_workers_video_confirmVideoUpload>): Promise<ResponseType<typeof api_workers_video_confirmVideoUpload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/video/confirm`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_video_confirmVideoUpload>
        }

        public async createWorkerReference(params: RequestType<typeof api_workers_references_createWorkerReference>): Promise<ResponseType<typeof api_workers_references_createWorkerReference>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/references`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_references_createWorkerReference>
        }

        public async deleteWorkerAvatar(): Promise<void> {
            await this.baseClient.callTypedAPI(`/workers/avatar`, {method: "DELETE", body: undefined})
        }

        /**
         * Deletes a compliance document for the authenticated worker.
         */
        public async deleteWorkerDocument(params: { documentId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/workers/documents/${encodeURIComponent(params.documentId)}`, {method: "DELETE", body: undefined})
        }

        public async deleteWorkerReference(params: { referenceId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/workers/references/${encodeURIComponent(params.referenceId)}`, {method: "DELETE", body: undefined})
        }

        public async deleteWorkerResume(): Promise<void> {
            await this.baseClient.callTypedAPI(`/workers/resume`, {method: "DELETE", body: undefined})
        }

        /**
         * Removes the worker's intro video.
         */
        public async deleteWorkerVideo(): Promise<void> {
            await this.baseClient.callTypedAPI(`/workers/video`, {method: "DELETE", body: undefined})
        }

        /**
         * Returns a ZIP archive of all compliance documents for a worker, accessible only by an employer with an accepted offer.
         */
        public async downloadWorkerDocumentsZip(params: { workerId: string }, options: PickMethods<"GET"> = {}): Promise<globalThis.Response> {
            options.method ||= "GET";
            return this.baseClient.callAPI(`/employers/workers/${encodeURIComponent(params.workerId)}/documents/download-zip`, options)
        }

        public async getAvatarUploadUrl(params: RequestType<typeof api_workers_avatar_getAvatarUploadUrl>): Promise<ResponseType<typeof api_workers_avatar_getAvatarUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/avatar/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_avatar_getAvatarUploadUrl>
        }

        /**
         * Generates a short-lived signed download URL for a specific worker document.
         * Only accessible by an employer with an accepted offer for that worker.
         */
        public async getDocumentDownloadUrl(params: { workerId: string, documentId: string }): Promise<ResponseType<typeof api_workers_employer_documents_getDocumentDownloadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/workers/${encodeURIComponent(params.workerId)}/documents/${encodeURIComponent(params.documentId)}/download-url`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_employer_documents_getDocumentDownloadUrl>
        }

        /**
         * Returns a signed upload URL for a compliance document.
         */
        public async getDocumentUploadUrl(params: RequestType<typeof api_workers_documents_getDocumentUploadUrl>): Promise<ResponseType<typeof api_workers_documents_getDocumentUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/documents/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_documents_getDocumentUploadUrl>
        }

        public async getReferencesForEmployer(params: { workerId: string }): Promise<ResponseType<typeof api_workers_references_getReferencesForEmployer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/workers/${encodeURIComponent(params.workerId)}/references`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_references_getReferencesForEmployer>
        }

        public async getResumeUploadUrl(params: RequestType<typeof api_workers_resume_getResumeUploadUrl>): Promise<ResponseType<typeof api_workers_resume_getResumeUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/resume/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_resume_getResumeUploadUrl>
        }

        public async getVerificationScore(): Promise<ResponseType<typeof api_workers_verification_score_getVerificationScore>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/verification-score`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_verification_score_getVerificationScore>
        }

        /**
         * Returns a signed upload URL for the worker's intro video.
         */
        public async getVideoUploadUrl(params: RequestType<typeof api_workers_video_getVideoUploadUrl>): Promise<ResponseType<typeof api_workers_video_getVideoUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/video/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_video_getVideoUploadUrl>
        }

        /**
         * Returns the authenticated worker's availability settings.
         */
        public async getWorkerAvailability(): Promise<ResponseType<typeof api_workers_availability_getWorkerAvailability>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/availability`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_availability_getWorkerAvailability>
        }

        public async getWorkerCompletion(): Promise<ResponseType<typeof api_workers_completion_getWorkerCompletion>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/completion`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_completion_getWorkerCompletion>
        }

        /**
         * Returns the authenticated worker's full profile.
         */
        public async getWorkerProfile(): Promise<ResponseType<typeof api_workers_profile_get_getWorkerProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/profile`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_profile_get_getWorkerProfile>
        }

        public async getWorkerResume(): Promise<ResponseType<typeof api_workers_resume_getWorkerResume>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/resume`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_resume_getWorkerResume>
        }

        /**
         * Returns the authenticated worker's current skill tags.
         */
        public async getWorkerSkills(): Promise<ResponseType<typeof api_workers_skills_getWorkerSkills>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/skills`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_skills_getWorkerSkills>
        }

        /**
         * Returns a signed playback URL for the authenticated worker's intro video.
         */
        public async getWorkerVideo(): Promise<ResponseType<typeof api_workers_video_getWorkerVideo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/video`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_video_getWorkerVideo>
        }

        /**
         * Returns the list of supported document types.
         */
        public async listDocumentTypes(): Promise<ResponseType<typeof api_workers_documents_listDocumentTypes>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/document-types`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_documents_listDocumentTypes>
        }

        /**
         * Returns the list of all supported skill tags.
         */
        public async listSkillTags(): Promise<ResponseType<typeof api_workers_skills_listSkillTags>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/skill-tags`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_skills_listSkillTags>
        }

        /**
         * Lists all compliance documents for the authenticated worker.
         */
        public async listWorkerDocuments(): Promise<ResponseType<typeof api_workers_documents_listWorkerDocuments>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/documents`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_documents_listWorkerDocuments>
        }

        /**
         * Lists a worker's compliance documents for an employer who has an accepted offer.
         * No signed URLs are returned here — metadata only. Signed URLs require a separate call.
         */
        public async listWorkerDocumentsForEmployer(params: { workerId: string }): Promise<ResponseType<typeof api_workers_employer_documents_listWorkerDocumentsForEmployer>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/employers/workers/${encodeURIComponent(params.workerId)}/documents`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_employer_documents_listWorkerDocumentsForEmployer>
        }

        public async listWorkerReferences(): Promise<ResponseType<typeof api_workers_references_listWorkerReferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/references`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_references_listWorkerReferences>
        }

        /**
         * Updates the expiry date of an existing compliance document.
         */
        public async updateDocumentExpiry(params: RequestType<typeof api_workers_documents_updateDocumentExpiry>): Promise<ResponseType<typeof api_workers_documents_updateDocumentExpiry>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                expiryDate: params.expiryDate,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/documents/${encodeURIComponent(params.documentId)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_documents_updateDocumentExpiry>
        }

        /**
         * Updates the authenticated worker's profile details.
         */
        public async updateWorkerProfile(params: RequestType<typeof api_workers_profile_update_updateWorkerProfile>): Promise<ResponseType<typeof api_workers_profile_update_updateWorkerProfile>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/profile`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_profile_update_updateWorkerProfile>
        }

        public async updateWorkerReference(params: RequestType<typeof api_workers_references_updateWorkerReference>): Promise<ResponseType<typeof api_workers_references_updateWorkerReference>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                notes:               params.notes,
                refereeEmail:        params.refereeEmail,
                refereeName:         params.refereeName,
                refereeOrganisation: params.refereeOrganisation,
                refereePhone:        params.refereePhone,
                refereeTitle:        params.refereeTitle,
                relationship:        params.relationship,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/references/${encodeURIComponent(params.referenceId)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_references_updateWorkerReference>
        }

        /**
         * Replaces the authenticated worker's skill tags with the provided list.
         */
        public async updateWorkerSkills(params: RequestType<typeof api_workers_skills_updateWorkerSkills>): Promise<ResponseType<typeof api_workers_skills_updateWorkerSkills>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/skills`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_skills_updateWorkerSkills>
        }

        /**
         * Creates or updates the authenticated worker's availability settings.
         */
        public async upsertWorkerAvailability(params: RequestType<typeof api_workers_availability_upsertWorkerAvailability>): Promise<ResponseType<typeof api_workers_availability_upsertWorkerAvailability>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/workers/availability`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_workers_availability_upsertWorkerAvailability>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | RequestType<typeof auth_auth>
  | Promise<RequestType<typeof auth_auth> | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.56.0)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: RequestType<typeof auth_auth> | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
